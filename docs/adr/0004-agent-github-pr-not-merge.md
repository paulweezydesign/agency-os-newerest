# Agents open PRs; humans merge

Executing teammate agents may push branches and open pull requests on linked **client** GitHub repositories. **Merging to protected branches stays human-owned** (GitHub protections + AgencyOS operators). Chosen over scaffold-only repos (too weak for “AI executes”) and agent-merge autonomy (too risky for production).
